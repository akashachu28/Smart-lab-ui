
  # AISense Dashboard Design

  This is a code bundle for AISense Dashboard Design. The original project is available at https://www.figma.com/design/J3C4VCcz8do04LFROnum5s/AISense-Dashboard-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  